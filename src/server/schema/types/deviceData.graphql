type DeviceInfo {
    id: Int!
    isAdmin: Boolean!
    name: String!
    userEmail: String!
    version: String!
    updatedDate: String
    subscriptionEnds: String
}

type GetDevicesResponse {
    results: [DeviceInfo!]!
    total: Int!
}