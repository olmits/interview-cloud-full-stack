type DeviceInfo {
    id: Int!
    isAdmin: Boolean!
    name: String!
    userEmail: String!
    version: String!
    updatedDate: String
    isLatestVersion: Boolean!
}

type GetDevicesResponse {
    results: [DeviceInfo!]!
    total: Int!
}